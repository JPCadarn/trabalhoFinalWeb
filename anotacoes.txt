TEMA:
	-> Site de Bebidas;
CLIENTE:
- Senha criptografada; //password_hash($senha, PASSWORD_BCRYPT);
- Valida senha com $passou = password_verify($senha, $criptografada);
- Enviar email de boas vindas após o cadastro;
- Área de pedidos;
- Área da conta;
- Carrinho;
- Consultas frete (ajax);
- Esqueci minha senha;
- Cadastros:
	-> Usuário;
	-> Endereço;
	-> Avaliação;
	-> Cartões;
_______________________________________________________
ADMINISTRATIVO
- Usuário administrativo (ADMIN, MASTER);
- Importar arquivos por arquivo externo;
- Visualizar histórico de compras;
- Colocar produtos em destaque;
- Visualizar produtos mais vendidos;
- Visualizar produtos mais acessados;
- Cadastros:
	-> Categoria; ook
	-> Produto; ook
	-> Produtos em Destaque; ook
_______________________________________________________
ToDo:
	-> Corrigir link da logo;
	-> Botão logar na navbar, se entrou coloca nome do usuário;
	-> Colocar footer em todas as páginas;
	-> Usar md5 p/ nomes das imagens;
	-> Ícone tab do navegador
	-> Categorias responsivo na navbar;
	-> Páginas de produtos;
	-> Login;
	-> Logs usuário;
	-> Cadastro de Usuário;
	-> View categorias admin;
	-> View produtos admin;
	-> View produtos em destaque admin;
	-> View usuários admin;
	-> View pedidos admin;
	-> Avaliações;
	-> Tratar erros;
	-> Esqueceu a senha;
	-> Email de boas vindas;
	-> Colocar efeito wave nos botões;
	-> Adicionar classe "hoverable" nos cards
	-> Ajustar inputs dos modals pra s12;
	-> Validações p/ não cadastrar mesmo email duas vezes;
SESSÃO:
<?php
    session_start();

    $_SESSION['logado'] = 'ok';

    //pode criar quantas chaves quiser

    unset($_SESSION['logado']);

    session_destroy(); //destroy os dados registrados na sessão
    session_unset(); //limpa todas as variáveis da sessão
    session_abort(); //descarta as alterações no array e encerra a sessão
?>